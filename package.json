{
  "name": "fastify-language-parser",
  "version": "0.3.0",
  "description": "Language parser plugin for fastify",
  "engines": {
    "node": ">=6"
  },
  "scripts": {
    "lint": "standard --fix",
    "test": " standard --fix && tap test/**/*.test.js --cov --no-browser",
    "coverage": "standard --fix && tap test/**/*.test.js --cov --no-browser --coverage-report html"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lependu/fastify-language-parser.git"
  },
  "keywords": [
    "nodejs",
    "fastify",
    "i18n"
  ],
  "author": "Akos Kovács <lependu@protonmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lependu/fastify-language-parser/issues"
  },
  "homepage": "https://github.com/lependu/fastify-language-parser#readme",
  "devDependencies": {
    "fastify": "^1.6.0",
    "standard": "^11.0.1",
    "tap": "^12.0.1"
  },
  "dependencies": {
    "accept-language-parser": "^1.5.0",
    "fastify-plugin": "^1.1.3"
  }
}
